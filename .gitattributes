# Keep every file byte-for-byte on checkout. Without this, Git on Windows
# rewrites line endings, which breaks the shell hook on macOS and makes these
# files differ from what the session-start hook fetches. Do not remove.
* -text
